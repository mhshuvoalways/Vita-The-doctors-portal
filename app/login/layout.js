export const metadata = {
  title: "VITA | Login",
  description: "Vita test app",
};

export default function ScheduleLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
